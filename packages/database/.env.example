# This DATABASE_URL is used for local development and running migrations from this package.
# In production, the connection string will be provided by the environment variables 
# of the specific application (e.g., auth-server, web-app) that consumes this package.
DATABASE_URL="postgresql://USER:PASSWORD@HOST:PORT/DATABASE"